<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Layout.Controls" xmlns:models="using:Layout.Models">

    <Style TargetType="local:RootDockControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RootDockControl">
                    <StackPanel>
                        <TextBlock Text="RootDockControl Title"></TextBlock>
                        <ContentPresenter 
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Margin="{TemplateBinding Padding}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContentTemplateSelector">
            <Setter.Value>
                <local:DockTemplateSelector RootDockTemplate="{StaticResource RootDockTemplate}"/>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
